# Project rules

## The Gallery.dc.html — entry page is LOCKED (18 Sep 2026)
Do not change the entrance overlay (the double doors, wood grain, brass pull
handles, eyebrow line, names, "Open the doors" trigger, "or skip to the
details" link, spacer, or their positions) unless the user explicitly asks for
an entry-page change. Approved by the user; treat as final.

## Working rules the user has asked for
- Change ONLY what is asked. No unrequested improvements, moves, or removals.
- Verify before claiming: check the file or the rendered DOM, never assert a
  change applied without confirming it.
- If an instruction is ambiguous, ask one short question rather than guessing.
- Prefer small targeted edits; bulk scripted edits have corrupted this file
  before.
